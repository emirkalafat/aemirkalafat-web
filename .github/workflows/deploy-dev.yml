name: Deploy to Dev Preview

on:
  push:
    branches:
      - dev

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build-only

      - uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_EMIRKLFTWEB }}

      - name: Deploy to Firebase Hosting (dev channel)
        run: npx firebase-tools@latest hosting:channel:deploy dev --project emirklftweb --expires 30d
